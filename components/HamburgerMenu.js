import { MenuIcon, MenuLine } from "../muistyles/Navbar.styles";

const HamburgerMenu = () => {
  return (
    <MenuIcon>
      <MenuLine />
      <MenuLine />
      <MenuLine />
    </MenuIcon>
  );
};

export default HamburgerMenu;
