import { styled } from '@mui/material/styles';

export const FormMsgShipProfile = styled('p')(({}) => ({
  width: '95%',
  fontSize: '1rem',
  color: 'red',
}));

export const formProfileShipStyles = {
  display: 'flex',
  flexDirection: 'column',
  alignItems: 'flex-start',
  width: '100%',
};

export const inputProfileShipStyles = {
  width: { xs: '100%', sm: '100%' },
  margin: '6px 0',
};

export const inputLastProfileShipStyles = {
  width: { xs: '100%', sm: '100%' },
  margin: '6px 0 20px 0',
};

export const addShipBtnStyles = {
  width: { xs: '100%', sm: '100%' },
};
